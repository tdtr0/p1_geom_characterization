name: geometric_transfer
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.2.0
  - pytorch-cuda=12.1
  - pip
  - pip:
    - transformers==4.40.0
    - accelerate==0.29.0
    - bitsandbytes==0.43.0
    - transformer-lens==1.14.0
    - numpy>=1.24.0
    - scipy>=1.10.0
    - scikit-learn>=1.3.0
    - h5py>=3.8.0
    - pandas>=2.0.0
    - matplotlib>=3.7.0
    - seaborn>=0.12.0
    - datasets>=2.14.0
    - wandb>=0.15.0
    - tqdm>=4.65.0
    - jupyter>=1.0.0
    - ipython>=8.12.0
    # Note: signatory requires manual installation after environment creation:
    # pip install signatory (needs torch installed first)
